[build]
builder = "nixpacks"
buildCommand = "cd ../.. && bun install && cd packages/api && bun run build"

[deploy]
startCommand = "bun run start"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
